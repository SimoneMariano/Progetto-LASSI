<%= form_with(model: secondhand) do |form| %>
  <% if secondhand.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(secondhand.errors.count, "error") %> prohibited this secondhand from being saved:</h2>

      <ul>
        <% secondhand.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :ISBN, style: "display: block" %>
    <%if secondhand.book_id.present? %>
      <%= form.text_field :ISBN,  value: Book.find(secondhand.book_id).ISBN %>
    <%else%>
      <%= form.text_field :ISBN %>
    <%end%>
  </div>


  <div>
    <%= form.label :image, style: "display: block" %>
    <%= form.file_field :image %>
  </div>

  <div>
    <%= form.label :description, style: "display: block" %>
    <%= form.text_field :description %>
  </div>

  <br>

  <div>
    <%= form.submit %>
  </div>
<% end %>
