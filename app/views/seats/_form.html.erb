<%= form_with(model: seat) do |form| %>
  <% if seat.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(seat.errors.count, "error") %> prohibited this seat from being saved:</h2>

      <ul>
        <% seat.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.number_field :name %>
  </div>

  <%if current_user.admin?%>
    <%= form.label :available, style: "display: block" %>
    <%= form.select :available, options_for_select([true, false]) %>
  <%end%>

  <br>

  <div>
    <%= form.submit %>
  </div>
<% end %>

