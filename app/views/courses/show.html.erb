<p style="color: green"><%= notice %></p>

<%= render @course %>

<div>
  <%= button_to "Delete course", @course, method: :delete, class: "btn btn-outline-danger", :data => {:confirm => 'Are you sure you want to delete this course of study?'} %>
</div>

<br>

<div>
  <%= link_to "Edit course", edit_course_path(@course), class: "btn btn-outline-primary" %> |
  <%= link_to "Back to library", library_path %>
</div>
