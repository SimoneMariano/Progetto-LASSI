<%= form_with(model: view_reservation) do |form| %>
  <% if view_reservation.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(view_reservation.errors.count, "error") %> prohibited this view_reservation from being saved:</h2>

      <ul>
        <% view_reservation.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
